from django.apps import AppConfig

class AnneeScolaireConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'annee_scolaire'
    verbose_name = "Gestion des Années Scolaires"